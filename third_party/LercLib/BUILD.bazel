package(default_visibility = ["//:__subpackages__"])

load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "LercLib",
    srcs = glob(
        ["*.cpp"],
        allow_empty = False,
    ),
    hdrs = glob(["*.h"]),
)
